<nav>
  <% if Current.user.present? %>
    <div class="d-flex gap-4 align-items-center">
      <%= link_to "Home", root_path, class: "btn btn-primary" %>
      <%= link_to "About", "#", class: "btn btn-secondary" %>
      <%= link_to "Contact", "#", class: "btn btn-secondary" %>
      <%= render Common::Button::BtnComponent.new(
        builder: "link_to",
        path: session_path,
        method: :delete,
        style: "light",
        other_styles: "ms-auto",
        data: { turbo_method: :delete }
      ) do %>
        Sign out
      <% end %>
       
      <%#= button_to "Sign out", session_path, method: :delete, class: "btn btn-light ms-auto" %>
    </div>
  <% else %>
    <div class="d-flex gap-2">
      <%= link_to "Se connecter", "#", class: "btn btn-primary" %>
      <%= link_to "S'inscrire", "#", class: "btn btn-secondary" %>
    </div>
  <% end %>
</nav>